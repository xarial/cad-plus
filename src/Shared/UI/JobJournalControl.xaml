<UserControl x:Class="Xarial.CadPlus.Plus.Shared.UI.JobJournalControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctrls="clr-namespace:Xarial.XToolkit.Wpf.Controls;assembly=Xarial.XToolkit.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="0" VirtualizingStackPanel.IsVirtualizing="True"
                 Margin="2" ItemsSource="{Binding Path=LogEntries}" />
        <Button Grid.Row="1" Margin="5" HorizontalAlignment="Right" Command="{Binding Path=ExportLogCommand}">
            <TextBlock Text="Export Log..." Margin="5"/>
        </Button>
    </Grid>
</UserControl>

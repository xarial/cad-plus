trigger:
  branches:
    include:
    - '!api-master'
    - '!api-dev'
  paths:
    include:
    - src/Shared/*

stages:
- template: templates/common-pipeline.yml
  parameters:
    projectPath: src/Shared/Shared.csproj